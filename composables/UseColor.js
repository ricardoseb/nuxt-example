export function useColor( theme ){
    let colors = [];
    const primary = 'primary';
    const secondary = 'secondary';
    const accent = 'accent';
    const neutral = 'neutral';

    switch(theme) {
        case "light":
            return colors.push()
        case y:
            // code block
            break;
        default:
        // code block
    }


}
